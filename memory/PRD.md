# PRD — SIPRO Property Development OS (lanjutan dari repo pandeyoga/dadada)

## Sesi 2026-09-10 (lanjutan #10) — Laporan Validasi Excel + Cek Konsistensi Silang (iteration 24) — SELESAI
- **Laporan validasi Excel** (`data_mgmt_full_report.py`, `GET /data-mgmt/full/sessions/{sid}/report.xlsx`, tombol `fulldata-download-report`): workbook berisi `_INDEX`, `_TEMUAN` (semua temuan + cek silang, error → peringatan → info, kolom Sheet/Baris/Kolom/Status/Tingkat/Pesan/Saran), dan sheet data (baris field & tipe tetap) dengan sel berwarna (error FECACA, peringatan FDE68A, info BAE6FD, berubah-dari-DB BBF7D0) + komentar sel berisi pesan & "→ saran". Parser melewati sheet berawalan `_` sehingga laporan bisa diunggah ulang sebagai sesi.
- **Cek konsistensi silang** (`data_mgmt_full_checks.py`, `report.checks`, `totals.checks`): dievaluasi pada kondisi setelah baris sheet diterapkan (AR di sheet `ar_invoices`, atau AR di DB yang deal-nya ada di sheet `deals`); kode: `AR_UNIT_SUM` (Σ termin unit ≠ unit_total), `AR_TOTAL_SUM`, `AR_PAID_SUM`, `AR_OUTSTANDING`, `AR_VS_DEAL` (Σ termin unit ≠ `deal.pricing.net_price`/`deal.price`), `AR_UNIT_MISMATCH`, `AR_DEAL_MISSING`, `AR_CONTRACT_UNIT`. UI: `CrossChecksPanel` (`fulldata-checks-panel`, item `fulldata-check-item-<code>-<key>`, "Lihat baris" melompat ke sheet/halaman), stat "Cek silang", peringatan di `CommitDialog` (`fulldata-commit-checks-warning`; impor tetap boleh).
- Verifikasi: testing agent iteration_24 — `tests/test_iter24_datamgmt_full_checks.py` 5/5 + regresi iter23 13/13, UI semua alur lulus.

## Sesi 2026-09-10 (lanjutan #9) — Ekspor & Impor SEMUA data (Excel) dengan viewer, feedback & saran koreksi (iteration 23) — SELESAI
- Repo `sisidjdy/sipro` di-clone ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID=org-sipro`, `PORTAL_MASTER_OTP`); dependensi dipasang (`emergentintegrations` terpisah); `memory/test_credentials.md` diisi.
- **Ekspor semua data** (`data_mgmt_full_schema.py`, `data_mgmt_full_export.py`, `GET /data-mgmt/full/collections`, `GET /data-mgmt/full/export.xlsx?collections=`): setiap koleksi Mongo ber-org (143; `file_blobs`, `data_backups`, `portal_otps`, `wa_webhook_events` dikecualikan) → satu sheet; baris 1 = nama field, baris 2 = tipe (str/int/float/bool/datetime/json), baris 3.. = dokumen; dict/list = JSON satu sel; `id` = kunci (`_id` hanya untuk koleksi tanpa `id`, mis. `counters`); sheet `_INDEX` memetakan sheet→koleksi; sel > 32.000 karakter ditulis `«TERLALU BESAR — tidak diubah»` dan tidak disentuh saat impor. Kelompok koleksi mengikuti `data_mgmt_purge.GROUPS/KEEP`.
- **Sesi impor** (`data_mgmt_full_session.py`, `data_mgmt_full_validate.py`, router `routers/data_mgmt_full_router.py`): `POST /sessions` (unggah xlsx → disimpan `BACKUP_DIR/<org>/import_sessions/<sid>.json` + `data_import_sessions`), `GET /sessions/{sid}` (laporan: totals + per sheet counts/issues/missing), `GET /sessions/{sid}/sheets/{sheet}?only_issues&status&offset&limit` (baris + `cells`, `status` insert/update/same/error, `changed`, `issues[{col,level,message,suggestion?}]`), `PATCH /sessions/{sid}/cells` (sunting sel → validasi ulang), `POST /sessions/{sid}/delete-rows`, `POST /sessions/{sid}/commit` (`mode` update|replace, `sheets`, `skip_errors`, `confirm:"IMPOR"`; snapshot otomatis `pra-impor-semua-data`; update = `$set` hanya field yang berubah; insert id otomatis; replace menghapus dokumen org yang tidak ada di sheet — super admin & akun pelaku tak pernah dihapus; masterplan.recompute_stats bila proyek/cluster/blok/unit tersentuh), `DELETE /sessions/{sid}`.
- **Validasi & saran (hanya saran, diterapkan bila diklik)**: per tipe (JSON rusak → saran hasil `ast.literal_eval`; angka teks "Rp 1.250.000" → 1250000; pecahan pada int; Ya/Tidak → TRUE/FALSE; tanggal non-ISO → ISO via dateutil; teks yang di-angka-kan Excel → pulihkan nilai DB), per kolom (`org_id` ≠ org → error + saran; telepon → +62; email huruf kecil/tidak sah; rujukan `*_id` ke koleksi lain tidak ditemukan → peringatan; kolom pilihan (status/type/kind/…) nilai baru → peringatan + fuzzy `difflib`); kunci ganda dalam sheet → error; sheet tak dikenal → error sheet; kolom baru → peringatan sheet.
- **UI** tab "Semua Data (Ekspor/Impor)" (`DATAMGMT.tabFull`, komponen `components/datamgmt/full/*`): kartu ekspor per kelompok (checkbox koleksi, ekspor semua/terpilih); unggah → `ImportWorkbench` (totals, tab sheet berbadge, filter, `SheetGrid` sel disorot merah/kuning/biru + hijau bila berubah dari DB, popover sel: pesan, "Terapkan saran", textarea edit, "Simpan sel"; `IssuePanel` daftar temuan + tombol terapkan; "Terapkan semua saran (sheet ini)" hanya error/peringatan; hapus baris; pager 100 baris) → `CommitDialog` (mode, pilih sheet, lewati error, ketik IMPOR) → hasil + nama snapshot; daftar sesi sebelumnya bisa dibuka/dihapus.
- Verifikasi: `backend/tests/test_iter23_datamgmt_full.py` 13/13 (testing agent), UI semua alur lulus (iteration_23). Catatan testing agent: klik sel kadang terhalang popper Select filter di Playwright (alur alternatif via panel temuan normal) — bukan blocker.
- Pre-existing: `validate_compliance` finance_engine.py 823 > 800 baris.
- Backlog: unduh laporan validasi sebagai Excel (sel bermasalah diberi warna & komentar); ekspor bertahap (streaming) bila data produksi sangat besar; pembersihan otomatis sesi impor lama.


## Sesi 2026-09-09 (lanjutan #8) — Perbandingan di panel Dokumen kontrak · Adendum otomatis (iteration 22) — SELESAI
- **Perbandingan di kontrak**: `GET /contracts/{cid}/spr-compare` (izin `documents.view`, `_contract_scoped` → Sales hanya kontraknya). `SprComparePanel` menerima `url`, `compact`, `title`; `ContractDocuments` selalu memuat panel `contract-spr-compare` "Cek sebelum minta tanda tangan — SPR vs Tagihan Finance" (compact: verdict + baris beda; `spr-compare-toggle` membuka tabel penuh). `compare()` menambah `legacy_spr_count` (SPR generik lama disebut di verdict `belum_terbit`), `document.is_addendum`, `document.parent_doc_number`.
- **Adendum otomatis** (`spr_compare.ensure_addendum`, dipanggil dari `payment_scheme_engine.set_for_contract` & `ar_router.create_schedule`): bila dokumen TERTANDATANGANI terbaru dalam rantai (SPR_* + `ADENDUM_SPR`) berbeda dengan tagihan Finance → dokumen `ADENDUM_SPR` (draft) lahir: naskah "Label : lama → baru", `diff_rows`, `parent_document_id/parent_doc_number`, `amounts_snapshot` baru, `reason`, aktivitas deal; idempoten (draft belum ttd diperbarui, tidak kembar); bila angka kembali sama dengan dokumen ber-ttd → draft dihapus otomatis. Template `ADENDUM_SPR` di-upsert ke `document_templates` (`doc_code` ADD-SPR), `doc_layout.TARGETS` ditambah. Rantai `CHAIN_CODES` dipakai `compare()` & `sign_block_reason` (adendum yang cocok boleh ditandatangani). UI: badge `contract-doc-addendum-badge` pada baris dokumen, badge "Adendum dari …" di `spr-compare-doc`.
- Verifikasi: `tests/test_iter21_spr_compare.py` 7/7 (adendum lahir, idempoten, PDF, ttd, draft dibatalkan saat revert); gate 48 HIJAU; invarian LULUS; testing agent iteration_22 semua lulus.
- Backlog: `finance_engine.py` 823 > 800 baris; portal pembeli belum menampilkan add-on/piutang biaya; adendum untuk perubahan harga/add-on (bukan skema) belum dipicu otomatis (hanya saat ganti skema).


## Sesi 2026-09-09 (lanjutan #7) — Gate 48 selaras · Tombol SPR lama → generator resmi · Panel "SPR vs Tagihan Finance" (iteration 21) — SELESAI
- Repo `pandeyoga/dadada` di-clone ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID=org-sipro`, `PORTAL_MASTER_OTP`); dependensi dipasang; DB di-seed; `memory/test_credentials.md` diisi.
- **Gate 48 D7 selaras** (`payment_scheme_engine.set_for_contract`): booking fee/titipan yang dialihkan otomatis (`receipts.funding == 'deposit'`) BUKAN penghalang ganti skema; hanya penerimaan kas nyata yang menolak (400 "adendum"). Ganti skema kini juga menyusun ulang AR (`create_ar_for_deal replace=True`, titipan dialokasikan ulang) dan mengarahkan `deal.scheme_id` (+`scheme_history`) → SPR = skema = piutang. Gate `verify_payment_schemes.py`: D7 diperluas (D7g AR 14 termin, D7h paid tetap, D7i deal.scheme_id, D7f kas nyata ditolak), K9c/K9d statis diperbarui, D9 memilih kontrak dengan penerimaan kas nyata, cleanup menunggu `events_pending==0`. **GATE 48 HIJAU (65)**, invarian bisnis LULUS.
- **Tombol SPR lama ditutup**: `POST /documents {template_code:"SPR"}` → 400 (arahkan ke kontrak / Jadikan Pembeli). `DealsListTab.createSpr` → `GET /contracts/{cid}/documents/available` + `POST /contracts/{cid}/documents` (recommended_code); tombol hanya tampil untuk deal booked/completed ber-`contract_id` (tanpa kontrak → dialog Jadikan Pembeli). `CreateSprDialog` (Dokumen › Dokumen Transaksi) kini memilih KONTRAK dan menampilkan template/alasan blok (`spr-deal-select`, `spr-dialog-template`, `spr-dialog-blocked`, `spr-dialog-submit`).
- **Panel "SPR vs Tagihan Finance"** (`backend/spr_compare.py`, `GET /finance/ar/{deal_id}/spr-compare`): SPR menyimpan `amounts_snapshot` numerik saat terbit (`docgen.generate`); dibandingkan baris per baris dengan AR (termin unit, add-on) + invoice biaya INB: harga bersih unit, booking fee, jumlah & tiap termin (label/jumlah/jatuh tempo), total termin, add-on, biaya per komponen (developer_borne informatif; "belum ditetapkan & belum ditagih" = sementara, bukan selisih), total biaya, TOTAL. State: `cocok | beda | beda_sudah_ttd | belum_terbit | tanpa_kontrak`, `provisional`, `older_documents`. **Tanda tangan SPR ditolak 400** bila ada selisih (`sign_block_reason`). UI: `SprComparePanel` di sheet Detail Piutang (Finance › Piutang) — `spr-compare-panel[data-state]`, `spr-compare-verdict`, `spr-compare-row[data-match]`, `spr-compare-provisional`.
- Verifikasi: `backend/tests/test_iter21_spr_compare.py` 5/5; gate 48 HIJAU; invarian bisnis LULUS; testing agent iteration_21 semua lulus (backend + UI).
- Pre-existing & belum ditangani: `validate_compliance` finance_engine.py 823 > 800 baris; skema seed "Standar KPR (DP 20%)" berjenis cash_bertahap.
- Backlog P1: pecah `finance_engine.py` <800 baris; portal pembeli belum menampilkan add-on/piutang biaya terpisah; tampilkan ringkasan compare juga di panel Dokumen kontrak (opsional, pengguna memilih Finance saja).


## Sesi 2026-09-08 (lanjutan #6) — Jalur 1: SPR = skema yang dipilih = piutang Finance (iteration 20) — SELESAI
- Repo `Yoahafavaua/sipro` di-clone ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID=org-sipro`, `PORTAL_MASTER_OTP`); dependensi dipasang; DB di-seed; `memory/test_credentials.md` diisi.
- **Cacat 1 ditutup** (`engine._h_deal_booked`): AR saat booking dibuat dari `deal.scheme_id` (skema yang dipilih sales), bukan skema bawaan finance. `customer_convert.convert` juga meneruskan `scheme_id`.
- **Kontrak mengikuti skema deal**: `customer_convert.deal_scheme_kind/suggested_scheme` membaca `payment_schemes.kind` dari `deal.scheme_id` (tebakan lama hanya untuk deal tanpa `scheme_id`); `ensure_contract` menyimpan `payment_scheme_id/name` dari deal → `resolve_for_contract`/aktivasi memakai skema yang sama. Reservasi menandai `deal.scheme_explicit` (`POST /deals/reserve` + konversi penawaran): skema eksplisit → konversi jenis lain ditolak 400 "dipilih saat reservasi"; skema bawaan → `_repoint_deal_scheme` (deal diarahkan ke skema jenis kontrak, AR disusun ulang). `convert-preview` menambah `deal_scheme`, `deal_scheme_name`; UI `ConvertToCustomerDialog` mengunci Select (`convert-scheme-locked`).
- **docgen SPR satu sumber**: DP% = termin pertama AR (`split_terms/dp_percent`), bukan `payment.*.dp_pct`/`costs.dp_percent`; jumlah cicilan = jumlah termin − 1; kalimat aturan hanya untuk termin berbasis peristiwa (tidak ada lagi "saat kontrak diaktifkan" untuk termin bertanggal); add-on dicetak sebagai "Tagihan terpisah"; komponen biaya DINAMIS `{{cost_rows}}` dari snapshot `costs.components` (baris = invoice biaya Finance, developer_borne ditandai); `total_note`/peringatan "SEMENTARA" hanya bila komponen biaya PEMBELI kosong (`buyer_incomplete_labels`, PPh penjual dikecualikan). Template `_HEAD_SPR` versi naik otomatis (naskah tersunting admin tidak ditimpa). Block baru `skema_deal_beda`.
- **Gerbang per jenis skema**: setting `slik.scheme_kinds` (default `["kpr"]`) dipakai `docgen._spr_gates` (before_spr) & `lead_lifecycle.requirements` (before_booking) → SPR cash tidak lagi ditahan SLIK; setting `addon.spkt_scheme_kinds` (default 3 jenis): KPR → gerbang akad kredit, tunai → gerbang AJB.
- **GL**: `_gl_deposit_applied` melewati deal yang sudah dihapus paksa (jurnal yatim 2-1450/2-1400 dari deal uji tidak lagi lahir).
- **Regresi**: `backend/tests/test_spr_single_source.py` (3 jenis skema: AR/kontrak/gerbang/isi SPR dibandingkan angka per angka dengan AR, `payment_breakdown`, invoice biaya; + penolakan konversi jenis lain) 4/4 lulus; `scripts/repro_spr_scheme_mismatch.py` (reproduksi via HTTP). Gate 46 HIJAU (96), invarian bisnis LULUS, verify_settings PASS, data integrity PASS. Testing agent iteration_20: semua lulus.
- Catatan pre-existing (di luar cakupan): gate 48 `verify_payment_schemes.py` D7 merah karena booking fee kini otomatis dialihkan ke AR (`paid>0` menolak ganti skema); `validate_compliance` finance_engine.py 823>800 baris; skema seed "Standar KPR (DP 20%)" berjenis cash_bertahap (data seed).
- Backlog P1: tombol "SPR" lama di daftar deal (`DealsListTab.createSpr`) masih jalur dokumen lama; pecah `finance_engine.py` <800 baris; sesuaikan gate 48 D7 dengan pengalihan booking fee otomatis; portal pembeli belum menampilkan add-on/piutang biaya terpisah.

## Sesi 2026-09-08 (lanjutan #5) — Repo dipulihkan ulang; ADD-ON = komponen pembayaran TERPISAH (iteration 19) — SELESAI
- Repo `pandeyoga/dadada` di-clone ulang ke `/app` (workspace sebelumnya kosong); `.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID`, `PORTAL_MASTER_OTP`); dependensi dipasang (`emergentintegrations` terpisah); DB di-seed ulang; `memory/test_credentials.md` diisi. Pekerjaan pemisahan add-on dari sesi terputus TIDAK ada di repo → diimplementasikan ulang.
- **Mesin harga** (`quotation_engine`): `gross_price` = harga unit saja (add-on bukan bagian harga); termin & simulasi KPR dari `net_price` (unit − potongan harga); `addon_net_total` (add-on − potongan bersasaran `addon`, mis. override add-on gratis); `buyer_total = net + addon_net + biaya all-in pembeli`; `refresh_totals()` menghitung ulang total tanpa mengulang potongan komponen. `payment_breakdown` urutan: UNIT_PRICE → potongan harga → NET_PRICE ("dasar termin / KPR", `kpr_base`) → ADDON:* → ADDON_TOTAL (`kpr_excluded`) → COST/COSTDISC → COST_TOTAL → TOTAL → BOOKING_FEE → AFTER_BOOKING_FEE; `kpr_base` disimpan.
- **Finance** (`finance_engine`): AR memuat baris add-on terpisah per add-on (`basis='addon'`, `kpr_excluded=true`, jatuh tempo `addon.due_days` default 30 hari — setting baru); `unit_total`/`addon_total` di AR; `kpr_outstanding()` = sisa termin unit saja; `_allocate(skip_kpr_excluded)` — pencairan KPR (`kpr_disburse`) tidak pernah melunasi baris add-on; deal lama (tanpa `pricing.addon_net_total`) tidak ditambah baris add-on (harga lama sudah memuatnya). `GET /finance/ar` & `/finance/ar/{deal}` menambah `unit_total`, `addon_total`, `kpr_outstanding`; breakdown menambah `kpr_base`, `addon_net_total`.
- **Seed**: promo `PROMO-ALLIN27` (Rp27jt dipotong komponen BPHTB) ditambahkan ke `pricing_engine.seed_defaults`.
- **UI**: kartu deal Lead › Unit & SPR memisahkan Harga unit / Add-on / Biaya all-in / Total (`lead-deal-components/addon/allin/total`); sheet detail AR: `ar-unit-addon-split`, baris termin `ar-item` + badge `ar-item-kpr-excluded` "tagihan terpisah · bukan KPR"; kolom Total tagihan pembeli sub-teks "unit … + add-on … + biaya …"; `QuotationBreakdown` (dialog reservasi/penawaran): "Harga bersih unit — dasar termin / KPR", add-on setelahnya bertanda "tagihan terpisah, tidak masuk KPR", "Biaya all-in pembeli", "Total dibayar pembeli (unit + add-on + biaya all-in)", header "Σ termin unit … · add-on & biaya all-in ditagih terpisah"; `PaymentBreakdown` badge "dasar KPR"/"bukan KPR".
- Verifikasi: testing agent iteration_19 — backend 100% (`tests/test_iter19_addon_separation.py` 6/6, `test_iter18_booking_allin.py` 6/6 disesuaikan ke model baru, `test_iter19_extra.py` 3/3), UI 100%.
- Backlog: portal pembeli belum menampilkan baris add-on/piutang biaya secara terpisah; dokumen SPR/kontrak (`docgen`) masih memakai `gross_price` kontrak (unit + add-on) — sudah benar secara total, belum memberi catatan "add-on bukan dasar KPR"; baris add-on SPK belum punya `step_code`.


## Sesi 2026-09-08 (lanjutan #4) — Breakdown booking + promo all-in sinkron ke Finance; hapus paksa; SPK dari unit — SELESAI
- Repo `pandeyoga/dadada` di-clone ulang ke `/app`; `.env` dilengkapi; `memory/test_credentials.md` diisi.
- **Mesin harga** (`quotation_engine.attach_costs` + `build_payment_breakdown`): SATU daftar komponen pembayaran pembeli (harga unit, add-on, potongan harga, harga bersih, komponen biaya all-in kotor − potongan = bersih, total dibayar pembeli, booking fee, sisa setelah BF). Reservasi memakai `simulate(allin_scheme_id=…)` yang sama → `by_target['cost:*']`, `cost_discount_amount`, `buyer_total`, `payment_breakdown` tersimpan di `deal.pricing`; komponen kpr_only konsisten dengan skema bayar.
- **Finance sinkron**: `ar_invoices.breakdown` (`finance_engine.ar_breakdown`); invoice biaya INB per komponen (bersih potongan, item membawa `gross/discount`) terbit OTOMATIS saat `deal.booked` (`allin_engine.deal_as_contract`), diikat ke kontrak saat kontrak lahir (`link_cost_docs_to_contract`); `GET /finance/ar` menambah `cost_total/cost_outstanding/buyer_total/cost_invoice_numbers`; `GET /finance/ar/{deal}` menambah `cost_invoices`. UI: kolom "Total tagihan pembeli" & "Piutang biaya", sheet detail AR menampilkan `PaymentBreakdown` + piutang biaya per komponen.
- **Kontrak**: `build_breakdown` baris `PROMO_DISCOUNT` = potongan harga dari snapshot, baris baru `COST_DISCOUNT` (potongan biaya, informatif — komponen sudah bersih), `cost_discount/total_discount/addon_total` di ringkasan.
- **Hapus paksa** (`force_delete.py`, hanya owner/super_admin + alasan ≥10 huruf, audit `force_delete`): `DELETE /deals/{id}?reason=` (cascade AR, kuitansi, jurnal, BF, titipan, biaya, kontrak, komisi, pajak, KPR, kupon dilepas; unit → available), `GET /deals/{id}/delete-check`, `DELETE /finance/ar/receipts/{rid}?reason=` (alokasi termin/titipan dibalik), `?force=true&reason=` pada DELETE lead/customer/unit/project. UI: `DeleteEntityButton` mode paksa (textarea alasan), tombol Hapus di daftar deal, tombol hapus pembayaran di detail AR.
- **SPK**: tombol "Buat SPK" kini membuka dialog berbasis unit master + RAB (`SpkFromRabDialog`; baris dari RAB tipe + add-on deal; step_code → lingkup jadwal → progres/termin); form bebas dipindah ke "SPK bebas (tanpa unit)". `sync_unit_binding` mendenormalisasi `units.addons/addon_codes` & `build_schedules.addons` (badge add-on di picker unit SPK).
- Verifikasi: testing agent iteration_18 — backend 6/6 (`backend/tests/test_iter18_booking_allin.py`), UI 100%.
- Backlog: baris add-on SPK belum punya `step_code` (tidak otomatis masuk jadwal bangun) → butuh pemetaan add-on ⇄ langkah template; pembeli hanya melihat piutang unit di portal (piutang biaya belum ditampilkan di portal).

## Sesi 2026-09-08 (lanjutan #3) — RAB ambil langkah dari template jadwal (fuzzy + pilih manual); Alamat Kantor Pemasaran — SELESAI
- **RAB ↔ Template Jadwal**: pencocokan tipe kini longgar (`build_engine.type_key_match`: huruf/angka saja + containment → "30/60" ⊂ "Rumah Tapak 30/60", "TIPE-36-72" = "Tipe 36/72"), dipakai `template_matches`, `rab_engine._known_steps`, filter `/build/templates?unit_type=`. Editor RAB menambah dropdown **"Template jadwal sumber langkah"** (`rab-template-source`, default Otomatis; bila tak ada yang cocok tampil hint `rab-template-source-hint` dan pemakai memilih template manual) — panel langkah tanpa RAB & dropdown langkah mengikuti pilihan itu.
- **Alamat kantor pemasaran**: field `marketing_office` di Proyek (`ProjectCreate`/`ProjectUpdate`, form tambah & ubah proyek: `project-form-marketing-office`, `project-edit-marketing-office`); `AppointmentDialog.defaultLocation` → "Kantor pemasaran {nama} — {marketing_office}" (fallback lokasi proyek).
- Verifikasi: testing agent iteration_17 — backend 6/6, UI 100%.

## Sesi 2026-09-08 (lanjutan #2) — Hapus data master + RBAC delete; lokasi survei ikut proyek — SELESAI
- **Hapus data master** (`backend/master_delete.py`): lead, customer, unit, proyek bisa dihapus bila TIDAK punya jejak transaksi (deal aktif, kontrak, invoice AR, pembayaran, SPK/PO/jurnal, pekerjaan terverifikasi, serah terima, KPR); turunan catatan (aktivitas, tugas, notifikasi, jadwal tanpa bukti, inspeksi/punch, cluster/blok/fase/izin/site plan untuk proyek) ikut dibersihkan. Endpoint: `DELETE /leads/{id}`, `DELETE /customers/{id}`, `DELETE /projects/{pid}` (baru), `DELETE /projects/{pid}/units/{uid}` (tak lagi terbatas status available) + `GET …/delete-check` → `{can_delete, blockers}`.
- **RBAC**: aksi `delete` di matriks — projects/units (project_manager, + `create` units), customers (sales_manager); leads sudah ada. `rbac.ensure_matrix_migrations()` (dipanggil di lifespan) menambahkan izin baru ke baris matriks yang sudah tersimpan di DB (idempoten, kunci `migrations`), karena baris tersimpan menang total atas bawaan.
- **UI**: komponen `DeleteEntityButton` (cek jejak dulu → tampilkan alasan bila terblokir) di Unit 360, profil Lead, profil Customer, halaman & sheet Proyek; tombol hanya tampil bila `can(resource,'delete')`.
- **Lokasi survei** (`AppointmentDialog`): teks tetap "Kantor pemasaran Cluster Asri" dihapus → dropdown proyek (`lead-appt-project`) dari Master Proyek, lokasi bawaan "Kantor pemasaran {nama proyek} — {lokasi}" (bisa diubah); `project_id` disimpan di appointment (`AppointmentCreate.project_id`).
- Verifikasi: testing agent iteration_16 — UI 100%, backend 10/10 (`backend/tests/test_iter16_delete_rbac.py`) setelah migrasi matriks.

## Sesi 2026-09-08 (lanjutan) — Lingkup Peran Bawaan + Langkah Tanpa RAB — SELESAI
- **Lingkup Peran Bawaan**: admin bisa mengubah lingkup data (all/own/project) peran bawaan non-akses-penuh dari Hak Akses. Backend: timpaan disimpan di `permission_settings` key `role_scope_overrides` (bukan koleksi `roles`, agar peran bawaan tak tampak kustom), dimuat bersama snapshot `load_custom_roles`; `role_scope()` membaca timpaan → semua `is_scoped_sales/is_project_scoped/scope_query/project_query` ikut otomatis (berlaku permintaan berikutnya tanpa login ulang). `PUT /admin/roles/{bawaan}` hanya menerima `scope` (null/bawaan = hapus timpaan); Direksi/Super Admin ditolak. `GET /admin/roles` menambah `scope_default`, `scope_overridden`. `build_calendar_view` dipindah dari `has_role(PROJECT_SCOPED_ROLES)` ke `is_project_scoped`. UI: tombol pensil `admin-roles-edit-scope` pada peran bawaan → `RoleFormDialog scopeOnly` (hanya pilihan lingkup, penanda "(bawaan)"), badge `admin-roles-scope-overridden` "diubah".
- **Langkah Tanpa RAB**: `GET /rab/templates/unit_type` menambah `steps_without_rab` (langkah template jadwal tipe yang belum punya baris RAB); pencocokan tipe RAB↔template jadwal kini toleran nama/kode (`_known_steps`, `_templates_for_type`, `rab_step_warnings_for_template`). UI: badge kuning `rab-type-steps-without-rab` di daftar RAB tipe; editor RAB memuat panel `rab-template-missing-steps` dengan chip per langkah (`rab-template-missing-step`, klik = tambah baris terisi kode/uraian/kategori/step_code) dan tombol "Tambah semua sebagai baris".
- Verifikasi: testing agent iteration_15 hijau (backend 11/11 pytest, UI 100%; termasuk uji penegakan: Sales melihat semua lead saat lingkup 'all', kembali menyempit saat 'own').

## Sesi 2026-09-08 — repo pandeyoga/dadada dipulihkan; 3 bug produksi ditutup — SELESAI
- Repo `pandeyoga/dadada` di-clone ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID`, `PORTAL_MASTER_OTP`); dependensi dipasang; `memory/test_credentials.md` diisi.
- **Bug 1 — Template Jadwal tidak "terkoneksi" ke unit (Mulai bangun selalu terblokir)**: Unit 360 › Pembangunan kini punya tombol **"Buat jadwal dari template"** (`unit-build-generate-btn`) pada kondisi kosong, membuka `GenerateScheduleDialog` dengan unit terpilih; kotak kosong menampilkan template yang cocok (`unit-build-template-chip`) atau peringatan bila tipe belum tercakup template. Backend: `build_engine.template_for_unit` kini mencocokkan nama **atau** kode tipe (`unit_type_keys`, `template_matches`, `expand_type_keys` via master `unit_types`, case-insensitive; template tanpa tipe = berlaku semua; template bertipe diprioritaskan). `GET /build/unit/{id}` tanpa jadwal mengembalikan `matching_templates` + `buildable`; `GET /build/templates?unit_type=` toleran kode/nama; `/build/unscheduled` menyertakan `unit_type_code`; grup reference `unit_type` menambah sumber `unit_types.name` (tipe master tampil di picker template walau belum ada unit).
- **Bug 2 — Pratinjau PDF Template Dokumen gagal di produksi**: akar masalah nginx resmi mengirim `.mjs` sebagai `application/octet-stream` → module worker pdf.js ditolak. `DocumentPreview.js` kini membundel worker lewat webpack (`new Worker(new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url))` → chunk `.js` ber-hash) + fallback ke `/pdf.worker.min.mjs` + pesan galat yang menampilkan sebab; `deploy/nginx.conf` menambah `location ~* \.mjs$ { default_type text/javascript; }`. **Perlu rebuild frontend di VPS** (`bash deploy/update.sh`).
- **Bug 3 — Peran kustom tidak muncul di dropdown Pengguna**: kamus reference dimuat sekali saat login → `RolesManager.afterChange` kini memanggil `reload()` reference, `AdminUsers` menyegarkan reference saat dibuka, dan `RoleSelect` selalu menyertakan nilai peran aktif. Edit lingkup data peran kustom diverifikasi berfungsi (PUT `/admin/roles/{code}`); peran **bawaan** memang tidak bisa diubah lingkupnya (by design).
- Verifikasi: testing agent iteration_14 — UI 3/3 alur hijau, backend 9/9 (`backend/tests/test_iter14_bugs.py`) setelah perbaikan `expand_type_keys`.

## Sesi 2026-09-07 (lanjutan #5) — repo znakskbd/sipro dipulihkan; gate merah ditutup; Cek Sinkron RAB–Jadwal — SELESAI
- Repo `znakskbd/sipro` di-clone ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID`, `PORTAL_MASTER_OTP`); `emergentintegrations` dipasang terpisah; DB di-seed ulang (`scripts/seed_reset.sh`); `memory/test_credentials.md` diisi 9 akun seed.
- **Gate merah sesi lalu ditutup**: `ux_audit` (badge `phase-synced-badge` kini ber-`data-phase-id`); `validate_compliance` — `finance_engine.py` 841→723 baris (Thin AP dipindah ke `finance_ap.py`, tetap di-re-export), `engine.py` 828→713 baris (5 sweeper dipindah ke `engine_sweepers.py`, tetap di-re-export; pemanggil tidak berubah).
- **Fitur baru — Cek Sinkron RAB–Jadwal** (permintaan pengguna): `rab_engine.step_sync_check` + `GET /api/rab/templates/unit_type/{ref}/sync-check`; `GET /rab/templates/unit_type` menambah `has_schedule_template` & `step_mismatch`; `PUT` RAB tipe mengembalikan `sync`; `GET/POST/PUT /build/templates` menambah peringatan "RAB tipe X memakai kode langkah … yang tidak ada di template ini". UI: `RabTypePanel` badge `rab-type-step-mismatch`; `RabTemplateDialog` banner `rab-template-sync-warning` + penanda baris `rab-template-step-mismatch` (dropdown merah, live saat diubah) + toast peringatan saat simpan; editor Template Jadwal menampilkan peringatan RAB di kotak "Perlu diperiksa".
- Ikutan: `seed.py` fase kawasan proyek demo kini membawa `work_category` (sinkron dari jadwal unit sejak seed); `recompute_project_progress` menandai `progress_source='unit_schedules'` walau angka tidak berubah.
- Verifikasi: testing agent iteration_13 hijau (pytest 11/11 `tests/test_iter13_ragjadwal_sync.py`, UI 100% skenario fitur baru); gate `ux_audit`, `validate_compliance`, `check_nav_map`, `verify_rbac_ui`, `audit_forms_deep`, `verify_field_names` hijau.
- Sisa non-blocking: warning console "<span> cannot be a child of <option>" berasal dari plugin visual-editor lingkungan dev (atribut `x-file-name`/`data-ve-dynamic`), bukan kode aplikasi.

## Sesi 2026-09-07 (lanjutan #4) — 4 temuan pengguna: tipe unit, skema all-in, survey, tahapan pembangunan — SELESAI
- **Tipe unit sinkron**: dialog "Buat unit" (Master Proyek › Struktur) memakai `UnitTypeField` = dropdown dari `/catalog/unit-types` + tombol "Tipe baru" yang membuka form master yang sama (`UnitTypeFormDialog`, dipakai juga `UnitTypePanel`); tipe baru langsung terpilih & muncul di Pusat Konfigurasi. `unit_type_ref` → `allow_new=False` (tidak ada lagi ketik bebas yang ditolak backend); `reference.py` mengirim `allow_new`.
- **Skema all-in sinkron & rapi**: `POST /cost-components` otomatis menambahkan komponen baru ke semua `allin_schemes` dengan `default_treatment` (`synced_schemes` di respons + audit). Editor skema menampilkan SEMUA komponen aktif sebagai baris tetap (Developer / Pembeli / Tidak termasuk + nominal pengganti); dialog `max-h-[90vh]` body scroll, footer selalu terlihat; kartu menampilkan nama komponen.
- **Survey**: poin/tahapan sudah bisa dikonfigurasi di Pusat Konfigurasi › Tahapan Survey (`survey_stage_configs`, survey baru menyalin snapshot); tautan "Atur poin & tahapan" ditambahkan di panel Survey Lokasi.
- **Tahapan pembangunan**: dua lapis — Pusat Konfigurasi › Tahapan Pembangunan (fase kawasan berbobot, `phase_templates`) dan Pembangunan › Template Jadwal (langkah per tipe unit W1-01…; `build_catalog`), RAB/BoQ menempel via `step_code`, SPK borongan menempel ke item pekerjaan. Tautan lintas ditambahkan di panel Tahapan Pembangunan.
- Verifikasi: testing agent iteration_11 hijau 5/5 + gate `ux_audit`, `check_nav_map`, `verify_rbac_ui`, `validate_compliance`, `audit_forms_deep`.

## Sesi 2026-09-07 (lanjutan #3) — pulihkan repo akaksjbd/sipro + tutup temuan iteration_9 — SELESAI
- Repo `akaksjbd/sipro` di-clone ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID=org-sipro`, `PORTAL_MASTER_OTP`); dependensi dipasang (`emergentintegrations` terpisah dari `requirements.txt`); DB di-seed ulang bersih (`scripts/seed_reset.sh` bagian seed). `memory/test_credentials.md` diisi 9 akun seed + `gudang@sipro.co.id` (peran kustom `admin_gudang` ← site_engineer, dibuat ulang via API).
- Temuan iteration_9 (satu-satunya): judul dialog pratinjau di `/admin/users` sempat menampilkan kode peran mentah selama izin dimuat → `AdminUsers.js` meneruskan `label={labelOf("user_role", role) || roleLabel(role)}` ke `RolePreviewButton` (dialog tambah & ubah pengguna).
- Verifikasi: testing agent iteration_10 hijau 5/5 (judul segera 'Pratinjau menu: Finance' / 'Site Engineer', regresi matriks & pratinjau admin_gudang, login peran kustom, smoke backend). Gate `ux_audit`, `check_nav_map`, `verify_rbac_ui`, `validate_compliance` hijau.
- Sisa non-blocking: warning Radix "Missing Description/aria-describedby" pada beberapa DialogContent admin (pre-existing).

## Sesi 2026-09-07 (lanjutan) — PERAN DINAMIS (Tambah Peran) — SELESAI
### Permintaan pengguna
"Sudah berapa kali menginstruksikan untuk bisa TAMBAH ROLE". Sesi-sesi sebelumnya menunda ini sebagai "keputusan K-2 belum dikonfirmasi"; kini dibangun penuh.

### Yang dibangun
- **Backend** `rbac.py`: koleksi `roles` (peran kustom: `code`, `label`, `description`, `inherits` = peran bawaan induk, `scope` = all/own/project, `is_active`); snapshot memori disegarkan tiap `get_matrix()` (TTL 5 s) & di lifespan. Fungsi baru `all_roles()`, `is_known_role()`, `inherits_of()`, `role_scope()`, `nav_role()`, `role_label()`. `is_scoped_sales`/`is_project_scoped`/`scope_query`/`project_query`/`assert_project_access` kini berbasis `role_scope` → peran kustom ikut aturan baris (own/project). `effective_matrix`/`validate_matrix` menerima peran kustom; warisan izin mengikuti `inherits`.
- **API** `admin_router.py`: `GET/POST /api/admin/roles`, `PUT/DELETE /api/admin/roles/{code}` (izin `permissions:manage`; kode `^[a-z][a-z0-9_]{2,31}$`, unik, induk hanya peran bawaan non-akses-penuh, hapus/nonaktif ditolak selagi dipakai pengguna, baris matriks peran ikut dibuang, audit log). `/admin/permissions` mengirim `roles` (bawaan+kustom), `role_meta`, `inherits` gabungan. `/admin/users` menerima peran kustom. `session_payload` (login/me) menambah `nav_role`, `role_label`, `role_scope`. Reference `user_role` dinamis dari koleksi `roles` (dropdown ikut memuat peran kustom).
- **Frontend**: `components/admin/RolesManager.js` (daftar peran bawaan+kustom, jumlah pemakai, tombol Tambah/Ubah/Hapus + konfirmasi) dan `RoleFormDialog.js` (nama → kode auto-slug, keterangan, mewarisi izin, lingkup data) di halaman Hak Akses; kolom peran kustom + badge "kustom" di matriks; `AdminUsers` memakai `/reference user_role`; sidebar memakai `nav_role` (peran kustom tanpa induk: menu murni dari izin); TopBar/Profil memakai `role_label`.
- Ikutan: `work_router._kpis` — peran kustom memakai set KPI induk; peran tanpa set khusus & bukan owner mendapat KPI pribadi (sebelumnya jatuh ke set org-wide → 403 di Beranda, juga menimpa `legal_admin`). `workhub.division_of/level_of` fallback ke peran induk. Routers `targets/analytics/quotations` memakai `is_scoped_sales`.
- **Uji**: `backend/tests/test_dynamic_roles.py` (5 lulus); testing agent iteration_7 — UI tambah/ubah/hapus peran, dropdown pengguna, login peran kustom (label, sidebar, Beranda 200), tolak hapus saat dipakai: semua hijau. Gate: `verify_rbac`, `ux_audit`, `check_nav_map`, `verify_auth_surface`, `validate_compliance` hijau; run penuh terakhir: `memory/verification/all-gates-2026-09-07e-roles.log`.

### Lanjutan sesi yang sama — Cabang Peran Literal + Salin Peran (SELESAI)
- `rbac.has_role(user, *roles)`: cocok bila peran pengguna ATAU peran induknya ada di daftar. 47 pengecekan literal `user.get("role") in/==/not in` di 24 berkas backend (build_*, procurement*, subcon*, boq, field, permits, inspection, stock, materials, complaints, deals override, data_mgmt, allin_amend, workhub, build_calendar_view) diganti `has_role`. Pemeriksaan `super_admin` murni (security/orgs/auth) dibiarkan literal — peran kustom memang tidak boleh mewarisi Direksi/Super Admin. Frontend: `ConstructionPage`, `PODetailSheet`, `NavMigrationDialog`, `ClaimOpnameSheet` memakai `user.nav_role || user.role`.
- **Duplikat peran**: tombol Duplikat (peran non-akses-penuh) → dialog terisi "X (Salinan)"/`x_salinan`, `POST /admin/roles` dengan `copy_from` menyalin izin EFEKTIF sumber (matriks+warisan+kode) menjadi baris matriks eksplisit peran baru (`copied_perms` dilaporkan; salinan Direksi/Super Admin ditolak 400).
- Perbaikan ikutan: `build_calendar_view.month_view` KeyError `total` saat pengguna tidak punya proyek yang bisa diakses (`days` belum melewati `_build_days`). Gate `verify_rbac_ui` mengenali pola `nav_role`.
- Uji: `test_dynamic_roles.py` 7 lulus; testing agent iteration_8 hijau (duplikat UI+API, kesamaan izin semua resource, has_role via `/build/calendar`); gate penuh: `memory/verification/all-gates-2026-09-07g-final.log`.

## Sesi lanjutan 2026-09-07 — pulihkan repo pandeyoga/dadada, tutup sisa gate merah
### Permintaan pengguna
Lanjutkan dari titik terhenti: gate `verify_cancellation_refund.py` merah pada run seed bersih (69/70), jalankan ulang sampai OVERALL PASS, rampungkan dokumen verifikasi + PRD. Clone ulang `pandeyoga/dadada` (main), pertahankan desain & stack.

### Yang dilakukan
- Repo diklon ulang ke `/app`; `backend/.env` dilengkapi (`JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID`, `PORTAL_MASTER_OTP`); `emergentintegrations` dipasang terpisah dari `requirements.txt` (pin `litellm` wheel internal tidak bisa dipasang bersamaan). `memory/test_credentials.md` (kosong di repo) diisi 12 akun seed.
- **Akar masalah gate merah "acak" pada seed bersih**: seed mengantre peristiwa domain (`spr_signed`, `full_payment`, `booking_fee_verified`, …) yang baru diproses tick `dispatch_pending` 8 detik SETELAH `/api/health` menjawab "ok". Gate yang berjalan lebih dulu membaca GL/kewajiban kontrak yang belum lengkap (bukti: `verify_business_invariants` 2-1400 GL 170 jt vs subledger 2,27 M; `verify_closing` K1b; lalu hijau saat diulang 60 detik kemudian). Bukan bug pembukuan.
- Perbaikan: `server.py` lifespan mengosongkan antrean peristiwa (`dispatch_pending` berulang, maks 20 putaran) SEBELUM server melayani; `/api/health` melaporkan `events_pending`; `scripts/seed_reset.sh` menunggu antrean kosong, menunggu MongoDB ping sebelum restart, dan me-restart backend sekali lagi bila startup gagal (ditemukan: `drop_database` sempat membuat `mongod` SIGABRT & di-spawn ulang supervisor, backend yang start di celah itu gagal startup dan reloader-nya diam).
- Hasil: `bash scripts/seed_reset.sh` → **OVERALL PASS (70 gates)** — `memory/verification/all-gates-2026-09-07b-clean.log`. Run ulang tanpa reset: lihat `all-gates-2026-09-07c-rerun.log` dan `memory/VERIFIKASI_SIPRO_2026-09-06.md` §2026-09-07.
- Gate 47 (`verify_cancellation_refund`) sendiri **hijau 110/110** pada seed bersih; kegagalan sesi lalu tidak dapat direproduksi setelah antrean peristiwa dikosongkan sebelum gate berjalan.

### Backlog (tidak berubah, lihat sesi 2026-09-06)
P1 fixture legacy mandiri, audit GL/BI non-lead & portal, matriks visual per peran, lencana data uji; P2 versi/diff naskah, tipografi, pemisahan template invoice/kwitansi.

## Sesi lanjutan 2026-09-06 — repo agavafayaja/sipro; editor dan re-verifikasi
### Permintaan pengguna sesi ini
Lanjutkan https://github.com/agavafayaja/sipro, cocokkan ulang AUDIT_SINTESIS_1.md dan plan lama dengan bukti valid; jangan asal mengubah. Tambahkan format naskah seperti Word (rata kiri/kanan/kanan-kiri) dan pengaturan posisi/urutan tabel; pastikan dokumen terkonfigurasi. Audit overlap all-in include/exclude, jarak hadap–Tenggara, popup, tabel, dan field. Pengguna menyetujui main/default branch serta mempertahankan desain.

### Persona dan kebutuhan tetap
- Owner/admin: konfigurasi dokumen/master tanpa mengubah kode, angka bisa ditelusuri, audit jujur.
- Sales: dokumen/peta unit terbaca dan kontrol yang sesuai hak akses.
- Keuangan: naskah resmi konsisten dengan nominal transaksi dan salinan pembeli.
- Pembeli: salinan dokumen yang sama, bukan versi cetak yang kehilangan tabel biaya.

### Implementasi dan keputusan arsitektur
- React/FastAPI/MongoDB dipertahankan; env platform tidak diganti. Tiptap untuk naskah berformat, PDF.js untuk menampilkan PDF backend.
- Naskah HTML tersanitasi di koleksi template existing, teks lama masih didukung; renderer ReportLab bersama untuk alignment/list/table. Nilai placeholder di-escape; token tabel diteruskan ke renderer.
- Tabel native bisa dipindah, baris/kolom diedit; posisi/lebar tabel terkonfigurasi. Ukuran cetak mengikuti margin/kertas.
- Naskah sistem KWITANSI/BKM/BKK/BAST/PENAWARAN/FAKTUR/BUPOT/LAPORAN dan INB tersambung ke penerbit; faktur/bupot ikut layout; konteks biaya salinan staf/portal dibagi lewat helper.
- Overlap all-in, kartu arah hadap/harga, input konfigurasi bertipe salah, dan beberapa warning notifikasi/grafik diperbaiki berdasarkan reproduksi.
- 48 tes terfokus lulus; 23 target preview PDF diuji. Laporan independen iteration_5 dan koreksinya dipisah dari hasil akhir. Rangkaian repo saat ini sebenarnya 70 gate, bukan 62.
- Bukti terperinci dan batas cakupan: `memory/VERIFIKASI_SIPRO_2026-09-06.md`; log di `memory/verification/`.

### Backlog terprioritas / langkah berikutnya
- P0: tutup regresi yang terbukti pada alur editor/PDF/UI sebelum penyerahan; hasil gate akhir dicatat di laporan verifikasi.
- P1: seluruh fixture legacy mandiri (sebagian masih tergantung seed), skip WA live yang jujur, audit GL/BI non-lead dan portal sampai sumber baris, matriks visual semua dialog/peran, lencana global data uji.
- P2: versi/diff naskah, tipografi lanjutan, pemisahan template turunan invoice/kwitansi bila diperlukan. Peran dinamis belum diputuskan pengguna.
- Tidak diklaim: seluruh audit akuntansi/keamanan/seluruh UI tuntas, atau WhatsApp live terbukti.

## Problem statement (asli, 2026-06)
Lanjutkan development repo. Temuan pemakai: kartu KPI Pipeline Lead salah (jumlah kartu melebihi total lead),
biaya all-in include/exclude & konfigurasi lain tidak ter-wiring ke fitur, template PDF tidak bisa mengatur posisi
tabel/urutan bagian, nomor surat SPR (3 jenis) harus bisa dikonfigurasi terpisah, banyak tabel master tanpa RBAC,
banyak input bebas yang seharusnya dropdown master (mis. CoA), latar site plan tidak tersimpan/tampil.

## Arsitektur
FastAPI (`/app/backend`, 100+ router) + React (CRA, shadcn) + MongoDB. Konfigurasi terpusat di `settings_store.py`
(Pusat Konfigurasi), penomoran `numbering_registry.py`, tampilan dokumen `doc_layout.py` + `pdf_layout.py`.
Env backend: `MONGO_URL, DB_NAME, JWT_SECRET, SEED_DEMO_USERS, BACKUP_DIR`.

## Temuan audit (2026-06-xx) → status
| # | Temuan | Status |
|---|---|---|
| 1 | KPI lead tumpang tindih (65 > 48); "Diam ≥7 hari" hitung lead baru; "Lewat SLA" query field tak tersimpan (`sla_state`) | ✅ kartu = partisi Total/Aktif/Menang/Daur ulang/Hilang; drilldown SLA pakai `stage_due_at`; idle syarat `created_at < cutoff`; count pakai `count_documents` |
| 2 | 12 kunci konfigurasi tidak pernah dibaca kode | ✅ 10 di-wire: `reservation.max_active_per_lead` + `override_roles` (deals/reserve), `require_booking_fee_before_spr`, `slik.gate`, `lead.required_demography` (gerbang SPR di `docgen.applicable`), `kpr.sla_days` (`kpr_stage_due_at`), `addon.require_spkt_for_excess_land`, `addon.excess_land_price_per_m2`, `doc.require_verification_default`, `ui.table_page_size` (session → useListQuery). 2 dihapus karena fiturnya tidak ada: `partner.portal_enabled`, `addon.excess_land_discount_needs_approval` |
| 3 | Tab "Baris & biaya" hanya berpengaruh di pratinjau; PDF asli mengabaikan; bagian tidak bisa diurutkan | ✅ `documents/{id}/pdf` memakai `money_rows_for` + `layout_amounts(breakdown)`; `render_letter` mengurutkan blok sesuai `sections.order`; penanda `{{tabel_biaya}}` di naskah; RowsForm punya panah urutan bagian. Bagian "biaya" mati secara bawaan (naskah SPR sudah memuat rincian inline) |
| 4 | Satu aturan `docnum` untuk semua SPR | ✅ aturan per jenis `docnum:SPR-CASH`, `docnum:SPR-CASHB`, `docnum:SPR-KPR`, `docnum:SPKT` (menimpa bawaan bila diubah) |
| 5 | Panel konfigurasi tanpa `can()`; master biaya/all-in/KPR digembok `settings` (owner only) | ✅ `EditGate` (fieldset disabled + spanduk) di ConfigCenter & MasterData; allin_router → resource `catalog` |
| 6 | CoA input teks bebas | ✅ `ReferenceSelect group=gl_account` di Komponen Biaya; kode komponen manual all-in → dropdown master |
| 7 | Latar site plan dibuang oleh `/site-plan/{id}` & showroom publik | ✅ payload memuat `background{url}`; `SvgPlanMap` merender `<image>` |

## Backlog / P1
- Halaman KPR: tampilkan `kpr_stage_due_at` (tersangkut) di UI pembiayaan.
- Uji regresi semua panel konfigurasi dengan peran finance/sales_manager (EditGate).
- Naskah SPR bawaan: opsi mengganti rincian inline dengan `{{tabel_biaya}}`.

## Kredensial uji
Lihat `/app/memory/test_credentials.md` (`scripts/seed_demo_users.py` untuk menambah akun demo).

## Sesi 2026-09-06 — Audit Sintesis Tahap 1–2 + Tahapan Pembangunan & Survey
Sumber: `AUDIT_SINTESIS.md` (32 temuan). Semua temuan di bawah **diverifikasi dulu di runtime** sebelum diperbaiki.

| ID | Temuan (terbukti) | Perbaikan |
|---|---|---|
| WA-02 | Termin lunas tetap jadi kandidat pengingat (`item.paid` tidak ada; item menyimpan `paid_amount`) | `wa_reminder_engine.candidates` baca `paid_amount` + skip `status=paid` |
| DOC-01 | Invoice PDF kolom Dibayar per termin selalu Rp 0 | `ar_router.invoice_pdf` baca `paid_amount` |
| WA-13 | Pengajuan template Meta tanpa `example` → INVALID_FORMAT | `meta_components` sertakan `example.body_text`/`header_text`; `submit` menolak variabel tanpa contoh; field `examples` di template + UI |
| WA-04 | Body ↔ variables tidak divalidasi | `validate_variables` di create/update template (400) & submit |
| WA-01 | 5 jenis pengingat memakai 1 template `payment_reminder` | 5 template `reminder_*` di-seed untuk semua org (`ensure_reminder_templates`), default setting per jenis diganti |
| WA-03 | Riwayat body ≠ yang dikirim (reason ditempel) | `_render` hanya isi template |
| WA-12 | Template `rejected` tampil "Menunggu" di Lead WA | `TemplatesPanel` pakai status asli + alasan Meta |
| Fitur | Fase proyek (`construction_phases`) tidak bisa dibuat dari UI, tanpa template | `phase_templates.py` + `/construction/phase-templates` CRUD + `/project/{id}/phases/apply` (idempoten); tab **Tahapan Pembangunan**; detail proyek: Terapkan template / Tambah fase |
| Fitur | Survey hanya checklist datar tanpa tahapan | `survey_stages.py` + `/survey-stages`; tab **Tahapan Survey** (tahap → poin, toggle wajib); survey baru menyalin tahapan; form survey = wizard per tahap; finalisasi ditolak bila poin wajib `na` |
| Anti-kambuh | Pola A/B/C | `memory/FIELD_MAP.md`, `scripts/verify_field_names.py`, `scripts/verify_audit_fixes.py` (masuk `run_all_gates.sh`), `backend/tests/test_audit_tahap12_tahapan.py` (6 lulus) |

Keputusan default yang dipakai (belum dikonfirmasi pemilik): K-1 satukan template WA → sesi lanjut; K-3 KWITANSI cukup; K-4 progres resmi = fase berbobot; K-5 ganti label DSO.

### Gate yang masih MERAH (pra-eksisting di commit 7da1bd5, bukan regresi sesi ini)
~~`ux_audit`, `audit_forms_deep`, `verify_ia_v2`/`verify_build_hub`/`verify_budget_target`, `verify_33`, `verify_p66`/`verify_contract_legal_docgen`, `verify_p67`, `verify_p75-78`, `verify_cancellation_refund`, `verify_panel_resilience`~~ — **semua HIJAU per 2026-09-06 (sesi lanjutan)**; lihat catatan Tahap 7 di bawah.

### Backlog berikutnya (sesudah Tahap 7)
- Tahap 7 §4 (setengah tersisa): ~~rumus tertulis & nama sama = rumus sama~~ ✅ (gate 62 D2–D3).
- Pemulihan suite pytest legacy yang bergantung data seed lama (unit tipe `TIPE-45-90` untuk `test_p80_rab`, kontrak/booking fee untuk `test_p69b/c`, `test_p91/92/93`); tes WA yang menuntut kredensial Meta (`test_p100_wa_setup`, `test_p94_95_wa`) diberi skip jujur bila kredensial kosong.
- Audit lanjutan area yang belum diaudit (AUDIT_SINTESIS Bagian 6): akuntansi/GL, metrik BI non-lead, portal pembeli.

### Backlog berikutnya (Tahap 3–7 audit)
- ✅ Tahap 3: satukan layar template WA di Pusat Konfigurasi (WA-14, WA-05..09, WA-07) — iteration_2.
- ✅ Tahap 4: `ACTION_META` label aksi RBAC (RBAC-02/03) — iteration_2. Peran dinamis bila K-2 = ya (belum).
- ✅ Tahap 5 (2026-09-06, iteration_3): DOC-02 target `INVOICE` sendiri (kind table, kategori penagihan; dipakai PDF invoice AR & invoice biaya all-in, pratinjau contoh invoice); CFG-03/04 semua setting enum wajib `ref_group` → `option_labels` dari `/api/reference` (grup baru `reference_p100.py`: lead_won_trigger, slik_gate, attribution_model, docnum_scope, docnum_reset_policy, deletion_request_status; `handover_settlement_policy` dipakai ulang), dropdown Aturan Bisnis menampilkan label + kode kecil; UI-02 `DeletionRequestsTable` pakai registry (gate `audit_forms_deep` E5 hijau); UI-01 testid statis di `.map()` diberi pembeda (`ux_audit` hijau). WA-09 tambahan: `WaTemplateCreate.code` opsional dihormati (test `test_crud_header_type` hijau). Tes: `tests/test_audit_tahap5_doc_cfg_ui.py`.
- ✅ Tahap 6 (sudah ada di kode saat repo dipulihkan 2026-09-06 sesi lanjutan; kini DIJAGA): CFG-01 `core_utils.period_of` satu definisi + WIB; FIN-01 drill-down AR = kartu (`paid_amount` per termin); FIN-02 `work_home` → `_sync_with_drilldown`; FIN-03 `outstanding_pct` (bukan "DSO" palsu); PRJ-01 pembagi = semua unit; PRJ-02 `construction_progress` resmi vs `units_progress` rekap; BI-01 `MIN_SOURCE_SAMPLE`; BI-02 win rate satu definisi + `conversion_pct`. Tes: `tests/test_audit_tahap6_angka.py` (17).
- ✅ Tahap 7 §2 (2026-09-06): **gate 62 `scripts/verify_card_drilldown.py`** (148 pemeriksaan) — kartu Beranda per peran, partisi Pipeline Lead, Keuangan (AR/AP/ember aging), BI (rumus tertulis, nama sama = rumus sama), penjaga kode — masuk `run_all_gates.sh`.
- ✅ Tahap 7 §3 (2026-09-06): `audit_forms_deep.py` E6 memeriksa `<SelectItem>` hardcode → 16 dropdown di 14 berkas diganti `<ReferenceItems group=…/>` (komponen baru `patterns/ReferenceItems.js`); grup SSOT baru `wa_inbound_type`.
- ✅ Gate merah pra-eksisting dibersihkan (2026-09-06): `verify_contract_legal_docgen` (K11c 3 kode penahan masuk Kamus Data `docgen_block`; fixture melalui SLIK berbukti lewat API nyata), `verify_p66` (probe tabel menyalakan bagian `biaya` yang kini mati bawaan), `verify_p67` (h1 `SimpleMarkdown` → `page-title`). `run_all_gates.sh` → **OVERALL PASS (62 gates)**.
- Lingkungan: `backend/.env` butuh `JWT_SECRET`, `SEED_DEMO_USERS=true`, `BACKUP_DIR`, `DEFAULT_ORG_ID`, `PORTAL_MASTER_OTP`; `emergentintegrations` dipasang terpisah (konflik `litellm` pin di requirements.txt). `tests/conftest.py` kini membaca URL dari `frontend/.env` dan mengekspor `REACT_APP_BACKEND_URL` untuk seluruh suite (banyak modul uji lama menyimpan URL pod yang sudah mati).
- Suite pytest legacy: sebagian tes bergantung data seed lama (`TIPE-45-90`, kontrak tertentu) atau kredensial Meta — bukan regresi; dicatat di backlog.
- Pre-existing gagal karena data lingkungan (bukan regresi): `test_iter149_legal.py::test_admin_update_user`, `test_p97_template_compliance.py::test_opt_out_blocks_marketing_not_utility`.
